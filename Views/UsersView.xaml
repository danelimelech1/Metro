<UserControl x:Class="Metro.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             FlowDirection="RightToLeft">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="&#x1F465; ניהול משתמשים" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource MahApps.Brushes.Accent}"
                       VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="+ הוסף משתמש" Command="{Binding AddUserCommand}"
                        Style="{StaticResource MahApps.Styles.Button.Square.Highlight}"
                        Margin="0,0,8,0" Padding="12,6"/>
                <Button Content="&#x21BB; רענן" Command="{Binding RefreshCommand}"
                        Style="{StaticResource MahApps.Styles.Button.Square}"
                        Padding="12,6"/>
            </StackPanel>
        </Grid>

        <!-- Search Bar -->
        <TextBox Grid.Row="1"
            mah:TextBoxHelper.Watermark="&#x1F50D; חיפוש לפי שם, שם משתמש או אימייל..."
            Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            FontSize="13" Height="36" Margin="0,0,0,12"/>

        <!-- Users Table -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  FontSize="13"
                  RowHeight="40"
                  mah:DataGridHelper.EnableCellEditAssist="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="שם מלא" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="שם משתמש" Binding="{Binding Username}" Width="120"/>
                <DataGridTextColumn Header="אימייל" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="תפקיד" Binding="{Binding Role}" Width="130"/>
                <DataGridTextColumn Header="נוצר בתאריך"
                    Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy}" Width="110"/>
                <DataGridTemplateColumn Header="סטטוס" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="8,3" HorizontalAlignment="Center"
                                    Background="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}">
                                <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStatusConverter}}"
                                           Foreground="White" FontSize="11"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="פעולות" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="&#x1F5D1;" ToolTip="מחק"
                                    Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    Background="Transparent" BorderThickness="0"
                                    FontSize="16" Cursor="Hand"
                                    HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
