<UserControl x:Class="Metro.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             FlowDirection="RightToLeft">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="&#x1F4CB; ניהול הזמנות" FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource MahApps.Brushes.Accent}"
                   Margin="0,0,0,16"/>

        <!-- Orders Table -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Orders}"
                  SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  FontSize="13"
                  RowHeight="44">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="לקוח" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="תאריך"
                    Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy}" Width="110"/>
                <DataGridTextColumn Header="סכום"
                    Binding="{Binding TotalAmount, StringFormat='₪{0:N2}'}" Width="120"/>
                <DataGridTemplateColumn Header="סטטוס" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="10,4" HorizontalAlignment="Center"
                                    Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}">
                                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="12"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
